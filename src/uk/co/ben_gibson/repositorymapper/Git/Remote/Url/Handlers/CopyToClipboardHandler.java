package uk.co.ben_gibson.repositorymapper.Git.Remote.Url.Handlers;

import uk.co.ben_gibson.repositorymapper.Git.Remote.Url.RemoteUrl;

/**
 * Takes a git remote host url and copies it to the clipboard.
 */
public class CopyToClipboardHandler implements UrlHandler
{
    @Override
    public void handle(RemoteUrl hostUrl)
    {

    }
}
